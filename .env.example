# Copy this file to .env for local development and fill in your actual values.
# The values provided here are defaults for the Docker Compose setup.

# --- General ---
# Used by the API and Processing services for AI features.
GEMINI_API_KEY=your_gemini_api_key_here

# --- Single User Credentials ---
# This is a single-user system. Set your username and password here.
LIFELOG_USERNAME=admin
LIFELOG_PASSWORD=your_secure_password_here

# --- API Service ---
# A strong, unique secret key for signing JWTs and other security functions.
# It's critical to change this for a production environment.
SECRET_KEY=your-super-secret-key-that-is-long-and-random
# Set to "true" to enable debug mode in the API service, providing more verbose error messages.
DEBUG=false
# A comma-separated list of allowed origins for CORS.
# This should be the URL of your frontend application.
ALLOWED_ORIGINS=http://localhost:5173

# --- Frontend ---
# The base URL for the API that the frontend will connect to.
VITE_API_BASE_URL=http://localhost:8000/api/v1

# --- PostgreSQL Database ---
# These variables are used by all services connecting to the database.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=lifelog
POSTGRES_PASSWORD=lifelogpassword
POSTGRES_DB=lifelog
# The full database connection URL, used by services that require it.
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# --- RabbitMQ ---
# These variables are used by all services connecting to the message queue.
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=user
RABBITMQ_PASS=password
# The queue used for ingesting raw events.
RABBITMQ_QUEUE=lifelog_events_queue
# The queue used for processing events.
RABBITMQ_PROCESSING_QUEUE=lifelog_processing_queue
