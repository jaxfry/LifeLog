[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    integration: marks tests that require a database connection
    unit: marks tests that do not require external services
filterwarnings =
    ignore::DeprecationWarning
