# .env.docker override for litellm Prisma
# This is required because Prisma expects a SQLAlchemy-less URL (no +asyncpg)
# and the main DATABASE_URL is for the Python server (SQLModel/SQLAlchemy)

DATABASE_URL=postgresql://lifelog:supersecretpassword@db:5432/lifelog_db
